<!-- home首页 -->
<view class="pages">
  <block wx:for="{{pages}}" wx:key="name">
    <!-- data-*   自定义属性 -->
    <button class="btn" type="primary" bindtap="onBtnClick" data-item="{{item}}">{{item.name}}</button>
  </block>
</view>